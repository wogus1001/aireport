spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aireport?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

solapi:
  api-key: ${SOLAPI_API_KEY:}
  api-secret: ${SOLAPI_API_SECRET:}
  sender: ${SOLAPI_SENDER:}
  template-id: ${SOLAPI_TEMPLATE_ID:}
